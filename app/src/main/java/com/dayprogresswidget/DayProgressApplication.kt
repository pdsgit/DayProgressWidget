package com.dayprogresswidget

import android.app.Application

/**
 * Application class for the Day Progress Widget app.
 * This serves as the entry point for the application and can be used
 * for initializing app-wide dependencies.
 */
class DayProgressApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Application initialization can be done here if needed
    }
}
